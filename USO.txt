╔══════════════════════════════════════════════════════════════════════════════╗
║                     WEB_RYAL — GUÍA DE SINCRONIZACIÓN                       								  ║
╚══════════════════════════════════════════════════════════════════════════════╝

Servidor: root@5.161.249.245  /  App: /root/app/
Local:    C:\Users\Lenovo\Documents\WEB_RYAL
Siempre usar PYTHONUTF8=1 en Windows para evitar errores de encoding.

══════════════════════════════════════════════════════════════════════════════
  ¿QUÉ QUIERO HACER? — ELIGE TU FLUJO
══════════════════════════════════════════════════════════════════════════════

  A. Primera importación de una categoría .............. ver sección A
  B. El proveedor actualizó: hay nuevos y quitó algunos  ver sección B
  C. Solo agregar productos nuevos, no tocar los viejos . ver sección C
  D. Actualizar solo una categoría (gorras, 1:1, etc.) .. ver sección D
  E. Calzado (yupoo) ................................... ver sección E

══════════════════════════════════════════════════════════════════════════════
  A. PRIMERA IMPORTACIÓN (categoría nueva o BD vacía)
══════════════════════════════════════════════════════════════════════════════

  Usa esto cuando nunca has cargado esta categoría antes o partes de cero.
  No hace falta reconcile porque no hay nada que desactivar.

  En LOCAL:
      PYTHONUTF8=1 venv/Scripts/python.exe scrape_modaverse_final.py --category gorra
      scp scraped_modaverse.json root@5.161.249.245:/root/app/scraped_modaverse.json

  En SERVIDOR:
      cd /root/app && source venv/bin/activate
      PYTHONUTF8=1 python config/manage.py load_productos --category gorra --no-images
      PYTHONUTF8=1 python config/manage.py import_images --only gorras --workers 6

══════════════════════════════════════════════════════════════════════════════
  B. ACTUALIZACIÓN — EL PROVEEDOR QUITÓ Y AGREGÓ PRODUCTOS
══════════════════════════════════════════════════════════════════════════════

  Usa esto cuando ya tienes productos en BD y Modaverse cambió su catálogo:
  algunos son nuevos, otros desaparecieron, otros solo cambiaron de subcat.

  PASO 1 — Activa mantenimiento (opcional pero recomendado)
      touch /root/app/maintenance.flag

  PASO 2 — Scrapear en LOCAL y subir JSON
      PYTHONUTF8=1 venv/Scripts/python.exe scrape_modaverse_final.py
      scp scraped_modaverse.json root@5.161.249.245:/root/app/scraped_modaverse.json

  PASO 3 — En SERVIDOR: cargar nuevos y reclasificar movidos
      PYTHONUTF8=1 python config/manage.py load_productos --no-images
      # ↑ crea productos nuevos, reclasifica los que Modaverse movió de subcategoría,
      #   salta los que ya existen. Nunca crea duplicados.

  PASO 4 — Revisar cuántos se desactivarían (dry-run)
      PYTHONUTF8=1 python config/manage.py reconcile_catalog --dry-run
      # ↑ muestra "scope=X · bajas=Y · reactivaciones=Z" sin escribir nada.
      #   Si Y > 30 % de X, el siguiente paso necesita --force.

  PASO 5 — Desactivar los que ya no están en Modaverse
      PYTHONUTF8=1 python config/manage.py reconcile_catalog
      # Si Y > 30 %:
      PYTHONUTF8=1 python config/manage.py reconcile_catalog --force

  PASO 6 — Decidir qué hacer con los desactivados
      Tienes dos opciones:

      Opción A — Conservar algunos, borrar el resto:
        1. Abre el panel admin → Productos → filtra is_active=No, auto_deactivated=Sí
        2. Activa manualmente los que quieras conservar (se les quita auto_deactivated)
        3. Borra permanentemente el resto:
           PYTHONUTF8=1 python config/manage.py reconcile_catalog --prune --dry-run
           PYTHONUTF8=1 python config/manage.py reconcile_catalog --prune

      Opción B — Borrar todos los desactivados sin revisar:
           PYTHONUTF8=1 python config/manage.py reconcile_catalog --prune

      Opción C — Dejarlos desactivados (ocultos pero en BD):
        No hacer nada. Seguirán en BD con is_active=False.
        reconcile_catalog los reactivará automáticamente si el proveedor los repone.

  PASO 7 — Descargar imágenes de los productos nuevos
      PYTHONUTF8=1 python config/manage.py import_images --workers 6

  PASO 8 — Desactivar mantenimiento
      rm /root/app/maintenance.flag

══════════════════════════════════════════════════════════════════════════════
  C. SOLO AGREGAR NUEVOS (el proveedor agregó pero no quitó nada)
══════════════════════════════════════════════════════════════════════════════

  Usa esto cuando el proveedor amplió su catálogo pero no retiró nada.
  load_productos salta automáticamente los productos ya existentes.

  En LOCAL:
      PYTHONUTF8=1 venv/Scripts/python.exe scrape_modaverse_final.py
      scp scraped_modaverse.json root@5.161.249.245:/root/app/scraped_modaverse.json

  En SERVIDOR:
      PYTHONUTF8=1 python config/manage.py load_productos --no-images
      PYTHONUTF8=1 python config/manage.py import_images --workers 6

══════════════════════════════════════════════════════════════════════════════
  D. ACTUALIZACIÓN POR CATEGORÍA (gorras, 1:1, G5, etc.)
══════════════════════════════════════════════════════════════════════════════

  Igual que B pero limitado a una categoría. Útil cuando solo quieres
  sincronizar gorras sin tocar camisetas u otras secciones.

  En LOCAL:
      PYTHONUTF8=1 venv/Scripts/python.exe scrape_modaverse_final.py --category gorra
      scp scraped_modaverse.json root@5.161.249.245:/root/app/scraped_modaverse.json

  En SERVIDOR:
      # Cargar nuevos y reclasificar subcategorías movidas
      PYTHONUTF8=1 python config/manage.py load_productos --category gorra --no-images

      # Revisar y aplicar bajas
      PYTHONUTF8=1 python config/manage.py reconcile_catalog --category gorra --dry-run
      PYTHONUTF8=1 python config/manage.py reconcile_catalog --category gorra --force
      # (--force porque Modaverse suele reorganizar gorras masivamente >30 %)

      # Conservar algunos antes de borrar: ver Paso 6 Opción A de la sección B

      # Borrar permanentemente los que ya no vende el proveedor
      PYTHONUTF8=1 python config/manage.py reconcile_catalog --category gorra --prune --dry-run
      PYTHONUTF8=1 python config/manage.py reconcile_catalog --category gorra --prune

      # Imágenes
      PYTHONUTF8=1 python config/manage.py import_images --only gorras --workers 6

══════════════════════════════════════════════════════════════════════════════
  E. CALZADO (Nike, Adidas, Air Jordan, etc.)
══════════════════════════════════════════════════════════════════════════════

  En LOCAL (scrapear y corregir precios):
      PYTHONUTF8=1 venv/Scripts/python.exe scrape_yupoo_pf.py --resume --with-detail
      PYTHONUTF8=1 venv/Scripts/python.exe fix_calzado_prices.py
      scp scraped_yupoo_pf.json root@5.161.249.245:/root/app/scraped_yupoo_pf.json

  En SERVIDOR (cargar BD e imágenes):
      PYTHONUTF8=1 python config/manage.py load_productos --only calzado --no-images
      PYTHONUTF8=1 python config/manage.py update_calzado_prices
      PYTHONUTF8=1 python config/manage.py download_yupoo_images

  NOTA: reconcile_catalog no afecta calzado (solo toca URLs de modaverse.vip).

══════════════════════════════════════════════════════════════════════════════
  SCRAPERS (ejecutar en LOCAL)
══════════════════════════════════════════════════════════════════════════════

  scrape_modaverse_final.py
  ─────────────────────────
  Scraper de gorras, camisetas, sudaderas y más desde modaverse.vip.
  En local usa Playwright (scrapling) para capturar XHR; en servidor usa
  GET directo a la API REST (scrapling es opcional).
  Salida: scraped_modaverse.json (ruta relativa al script — funciona en Windows y Linux)
  NOTA: La API solo devuelve 1 imagen por producto (limitación permanente).

      python scrape_modaverse_final.py                    # scrape completo
      python scrape_modaverse_final.py --list             # ver categorías disponibles
      python scrape_modaverse_final.py --category gorra   # solo gorras (fusiona con JSON)
      python scrape_modaverse_final.py --category g5 "1:1"  # varias categorías
      python scrape_modaverse_final.py --category gorra --no-merge  # sobreescribe JSON

  Categorías disponibles (2026-05-21):
      gorra       → Gorra (29 subcats)
      deportivas  → Camisetas deportivas y jerseys de fútbol (35 subcats)
      g5          → Camisetas/Sudaderas Calidad G5 (87 subcats)
      1:1         → Camisetas/Sudaderas Calidad 1:1 (41 subcats)
      electr      → Electrónica (1 subcat)
      zapato      → zapato (177 subcats — categoría nueva en modaverse)
      van cleef   → Van Cleef & Arpels (2 subcats — categoría nueva)

  scrape_yupoo_pf.py
  ──────────────────
  Scraper completo de calzado desde putianshoefactory.x.yupoo.com.
  Recorre 26 marcas paginando cada categoría. Extrae nombre, precio y fotos.
  Salida: scraped_yupoo_pf.json

      python scrape_yupoo_pf.py --resume              # agrega sin repetir existentes
      python scrape_yupoo_pf.py --with-detail         # entra a cada álbum (más fotos)
      python scrape_yupoo_pf.py --brands Nike Adidas  # solo esas marcas
      python scrape_yupoo_pf.py --limit 50            # máx 50 álbumes por marca

  NOTA: yupoo normalmente tiene 7-8 fotos por álbum. El límite interno es 250.

  fix_calzado_prices.py
  ─────────────────────
  Re-scrapea SOLO las páginas de listado de cada marca (rápido, ~10 min).
  Extrae el precio del título del álbum y actualiza scraped_yupoo_pf.json.
  Correr DESPUÉS de scrape_yupoo_pf.py para asegurar precios correctos.

      python fix_calzado_prices.py             # actualiza JSON
      python fix_calzado_prices.py --dry-run   # muestra cambios sin guardar

══════════════════════════════════════════════════════════════════════════════
  MANAGEMENT COMMANDS (ejecutar en SERVIDOR)
══════════════════════════════════════════════════════════════════════════════

  load_productos
  ──────────────
  Lee scraped_*.json y crea productos en BD. Deduplica por supplier_url.
  Pre-carga todos los URLs existentes en un set Python (1 query) → salta
  automáticamente los ya importados sin hacer queries individuales por producto.
  Al terminar informa: "X nuevos · Y ya existían (omitidos)".

      python manage.py load_productos                       # modaverse (solo nuevos)
      python manage.py load_productos --only calzado        # calzado yupoo_pf (solo nuevos)
      python manage.py load_productos --no-images           # sin descargar imágenes (recomendado)
      python manage.py load_productos --category "1:1"      # solo esa categoría (aplica tallas+colores + reclasifica subcats)
      python manage.py load_productos --fix-urls            # actualiza supplier_url → #/proinfo/

  import_images
  ─────────────
  Descarga imágenes modaverse en paralelo. Usa chunking de 300 para no
  saturar RAM.

  Modos (se excluyen entre sí, de menos a más agresivo):
    DEFAULT    → solo productos con 0 imágenes (no toca existentes)
    --fill-gaps → productos con < max_imgs imágenes (rellena huecos)
    --force    → todos los productos (re-descarga aunque ya tengan imágenes)

      python manage.py import_images                              # solo sin imágenes (default)
      python manage.py import_images --fill-gaps                  # rellena imágenes faltantes
      python manage.py import_images --only gorras                # limitar a una categoría
      python manage.py import_images --workers 6                  # hilos paralelos (default 3)
      python manage.py import_images --max-per-product 250        # máx imágenes por producto
      python manage.py import_images --since 2026-05-21           # filtrar por fecha de creación
      python manage.py import_images --fill-gaps --since 2026-05-21  # combinar
      python manage.py import_images --force                      # re-descargar todo

  download_yupoo_images
  ──────────────────────
  Descarga imágenes de calzado TN2 desde yupoo. Completa hasta 250 por producto.

      python manage.py download_yupoo_images
      python manage.py download_yupoo_images --since 2026-05-21   # solo productos nuevos

  update_calzado_prices
  ──────────────────────
  Lee scraped_yupoo_pf.json y actualiza base_price en BD por supplier_url.
  IMPORTANTE: base_price = precio real del proveedor (del título del álbum),
  NO el default por marca.

      python manage.py update_calzado_prices
      python manage.py update_calzado_prices --dry-run

  reconcile_catalog
  ─────────────────
  Reconcilia el catálogo local con scraped_modaverse.json:
    - Soft-delete (is_active=False + auto_deactivated=True) de productos removidos.
    - Reactivación de los que reaparecen (solo los que este comando desactivó antes).
    - No toca calzado/yupoo ni productos sin URL modaverse.
    - Requiere migración 0024 (python manage.py migrate catalog).
  IMPORTANTE: correr DESPUÉS de load_productos con el mismo JSON actualizado.

  Comportamiento con --category:
    - Scope DB  = todos los productos Django bajo esa categoría raíz (jerarquía Django completa).
    - Live pids = solo pids presentes en la sección actual del JSON para esa categoría.
    → Productos que ya no están en la sección activa de Modaverse quedan desactivados,
      aunque sigan existiendo en otras secciones del JSON (categorías legacy).
    → Si Modaverse reorganizó masivamente (>30 % de bajas), usar --force.

      python manage.py reconcile_catalog                           # full (todos modaverse)
      python manage.py reconcile_catalog --dry-run                 # previsualizar sin escribir
      python manage.py reconcile_catalog --category gorra          # solo gorras
      python manage.py reconcile_catalog --category gorra --dry-run --force  # ver plan sin umbral
      python manage.py reconcile_catalog --category gorra --force  # aplicar aunque >30 % bajas
      python manage.py reconcile_catalog --category gorra --prune --dry-run  # previsualizar borrado
      python manage.py reconcile_catalog --category gorra --prune  # borrar permanentemente
      python manage.py reconcile_catalog --max-deactivate-pct 50  # subir umbral al 50 %

  Guardas de seguridad (evitan borrar de más por scrape fallido):
    zero-guard  → aborta si el JSON trae 0 productos en el scope
    umbral 30 % → aborta si las bajas superan el 30 % de los activos en scope
    Ambas se saltan con --force.

  fix_corrupted_images
  ─────────────────────
  Re-descarga imágenes corruptas de modaverse CDN. Solo funciona si la URL
  original sigue activa.

      python manage.py fix_corrupted_images
      python manage.py fix_corrupted_images --min-size 5000  # umbral 5 KB
      python manage.py fix_corrupted_images --workers 4

  repair_images
  ─────────────
  Detecta y corrige imágenes con nombres incorrectos en BD/disco.
  Cubre dos casos:
    1. Path anidado por bug antiguo (ej. products/api.modaverse.vip/...)
    2. Extensión con sufijo extra (ej. XT_0102836.jpg_1774322093986)
       → detecta la extensión real (.jpg) y renombra a {SKU}_{orden}.jpg
       → borra el archivo original después de copiar

      python manage.py repair_images              # ejecutar reparación
      python manage.py repair_images --dry-run    # solo mostrar qué haría (sin cambios)
      python manage.py repair_images --redownload-missing  # también re-descarga sin imagen

══════════════════════════════════════════════════════════════════════════════
  UTILIDADES LOCALES
══════════════════════════════════════════════════════════════════════════════

  pack_images.py
  ──────────────
  Empaqueta imágenes locales buenas para reemplazar corruptas en el servidor.
  Lee corrupted_list.txt (nombres del servidor), hace match por SKU+índice
  ignorando el sufijo aleatorio de Django.
  Salida: good_images.tar.gz

  Flujo completo para limpiar imágenes corruptas en servidor:

    # 1. Servidor — generar lista con umbral 5 KB
    find /root/app/config/media/products -name "*.jpg" -size -5000c \
      -printf "%f\n" | sort > /root/app/corrupted_list.txt

    # 2. Local — bajar lista
    scp root@5.161.249.245:/root/app/corrupted_list.txt corrupted_list.txt

    # 3. Local — crear tar
    python pack_images.py   →   genera good_images.tar.gz

    # 4. Local — subir tar
    scp good_images.tar.gz root@5.161.249.245:/root/app/good_images.tar.gz

    # 5. Servidor — extraer y reasignar portadas
    cd /root/app/config/media/products
    tar -xzf /root/app/good_images.tar.gz
    rm /root/app/good_images.tar.gz

    # Eliminar registros corruptos de BD (si se prefiere borrar en lugar de reparar)
    python3 -c "
    lines = open('/root/app/corrupted_list.txt').read().strip().split('\n')
    vals = ', '.join(f\"'products/{l.strip()}'\" for l in lines if l.strip())
    print(f'DELETE FROM catalog_productimage WHERE image IN ({vals}) RETURNING id;')
    " | PGPASSWORD='WRyal2026!DB' psql -h 127.0.0.1 -U webryal_user webryal

    # Reasignar portadas a productos que perdieron su cover
    PGPASSWORD='WRyal2026!DB' psql -h 127.0.0.1 -U webryal_user webryal -c "
    UPDATE catalog_productimage pi SET is_cover = true
    FROM (
      SELECT DISTINCT ON (product_id) id FROM catalog_productimage
      WHERE product_id NOT IN (
        SELECT product_id FROM catalog_productimage WHERE is_cover = true)
      ORDER BY product_id, display_order
    ) sub WHERE pi.id = sub.id;"

══════════════════════════════════════════════════════════════════════════════
  MODO MANTENIMIENTO
══════════════════════════════════════════════════════════════════════════════

  Activa una pantalla 503 para visitantes. No requiere reiniciar Gunicorn.
  El staff (admin) puede seguir entrando al panel con normalidad.

  # Activar (poner cartel de mantenimiento)
  touch /root/app/maintenance.flag
  systemctl restart gunicorn

  # Desactivar (volver a producción normal)
  rm /root/app/maintenance.flag
  systemctl restart gunicorn

  NOTAS:
  - El cambio es inmediato (no hace falta restart gunicorn ni nginx).
  - Bypass automático para: usuarios is_staff=True, /admin/, /accounts/login/.
  - Útil antes de correr migraciones largas o subir un deploy grande.

══════════════════════════════════════════════════════════════════════════════
  CONSULTAS ÚTILES EN SERVIDOR
══════════════════════════════════════════════════════════════════════════════

  # Contar imágenes en disco y BD
  find /root/app/config/media/products -name "*.jpg" | wc -l
  PGPASSWORD='WRyal2026!DB' psql -h 127.0.0.1 -U webryal_user webryal \
    -c "SELECT COUNT(*) FROM catalog_productimage;"

  # Productos sin imagen
  PGPASSWORD='WRyal2026!DB' psql -h 127.0.0.1 -U webryal_user webryal -c "
  SELECT COUNT(*) FROM catalog_product p
  WHERE NOT EXISTS (SELECT 1 FROM catalog_productimage pi WHERE pi.product_id = p.id);"

  # Imágenes corruptas en disco (< 5 KB)
  find /root/app/config/media/products -name "*.jpg" -size -5000c | wc -l

  # Estado de imágenes por categoría
  PGPASSWORD='WRyal2026!DB' psql -h 127.0.0.1 -U webryal_user webryal -c "
  SELECT c.name, COUNT(DISTINCT p.id) productos, COUNT(pi.id) imagenes
  FROM catalog_product p
  JOIN catalog_category c ON p.category_id = c.id
  LEFT JOIN catalog_productimage pi ON pi.product_id = p.id
  GROUP BY c.name ORDER BY c.name;"

  # Reiniciar servidor tras cambios
  systemctl restart gunicorn

══════════════════════════════════════════════════════════════════════════════
  JSONS
══════════════════════════════════════════════════════════════════════════════

  scraped_modaverse.json   ← scrape_modaverse_final.py  (gorras/camisetas/sudaderas)
  scraped_yupoo_pf.json    ← scrape_yupoo_pf.py + fix_calzado_prices.py  (calzado)

══════════════════════════════════════════════════════════════════════════════
