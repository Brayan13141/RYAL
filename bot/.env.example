# Copiar a .env.persona1 y .env.persona2 y completar
SUPPLIER_GROUP_ID=120363XXXXXXXXXX@g.us
RYAL_GROUP_ID=120363XXXXXXXXXX@g.us
FORWARD_TO_RYAL=true
DJANGO_API_URL=http://localhost:8000
DJANGO_API_KEY=dev-negocio-key-insecure
MARKUP=100
# Puerto del servidor local /notify — DEBE ser único por instancia de bot en
# este mismo host (default 8952 choca si dos bots corren a la vez).
NOTIFY_PORT=
# JID del Grupo Pedidos — solo se configura en la instancia de pedidos (persona2).
ORDERS_GROUP_ID=
